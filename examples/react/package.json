{
  "name": "tygor-react-example",
  "private": true,
  "workspaces": ["client"],
  "scripts": {
    "postinstall": "go run . -gen -out ./client/src/rpc",
    "dev": "concurrently --kill-others -n tygor,vite -c green,blue \"go run github.com/air-verse/air@latest\" \"cd client && bun run dev\"",
    "build": "cd client && bun run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
