# Config Showcase

Demonstrates different TypeScript generation configurations side-by-side.

This example generates the same Go types with different config options to show how each setting affects the output. Unlike other examples, this one runs custom generation logic to produce multiple output variants.

## Generated Outputs

| Directory | Configuration | Description |
|-----------|--------------|-------------|
| `client/src/union/` | `EnumStyle("union")` | String union types (default, cleanest for most uses) |
| `client/src/enum/` | `EnumStyle("enum")` | TypeScript enums with member docs |
| `client/src/const/` | `EnumStyle("const_enum")` | Inlined at compile time |
| `client/src/object/` | `EnumStyle("object")` | Const objects, runtime accessible |
| `client/src/opt-default/` | `OptionalType("default")` | omitempty uses `?:`, pointers use `\| null` |
| `client/src/opt-null/` | `OptionalType("null")` | All optional fields use `\| null` |
| `client/src/opt-undef/` | `OptionalType("undefined")` | All optional fields use `?:` |
| `client/src/no-comments/` | `PreserveComments("none")` | No JSDoc comments |

## Quick Start

```bash
go run . gen    # Generate all TypeScript variants
```

Then compare the outputs in `client/src/*/types.ts`.

## Go Types

The example uses these types to demonstrate different generation options:

<!-- [snippet:status-enum] -->
```go title="types.go"
// Status represents the state of a task.
type Status string

const (
	// StatusPending indicates the task is waiting to be started.
	StatusPending Status = "pending"
	// StatusInProgress indicates the task is currently being worked on.
	StatusInProgress Status = "in_progress"
	// StatusCompleted indicates the task has been finished.
	StatusCompleted Status = "completed"
	// StatusCancelled indicates the task was cancelled.
	StatusCancelled Status = "cancelled"
)

```
<!-- [/snippet:status-enum] -->

<!-- [snippet:task-struct] -->
```go title="types.go"
// Task represents a work item in the system.
type Task struct {
	// ID is the unique identifier for the task.
	ID string `json:"id"`
	// Title is the task's headline.
	Title string `json:"title"`
	// Description is the detailed task description.
	Description *string `json:"description,omitempty"`
	// Status is the current state of the task.
	Status Status `json:"status"`
	// Priority determines the task's urgency.
	Priority Priority `json:"priority"`
	// Assignee is the user responsible for the task.
	Assignee *string `json:"assignee,omitempty"`
	// DueDate is when the task should be completed.
	DueDate *time.Time `json:"due_date,omitempty"`
	// Tags are labels for categorization.
	Tags []string `json:"tags"`
	// CreatedAt is when the task was created.
	CreatedAt time.Time `json:"created_at"`
}

```
<!-- [/snippet:task-struct] -->

## Output Comparison

### EnumStyle: union (default)

<!-- [snippet-file:client/src/union/types.ts] -->
```typescript title="types.ts"
// Code generated by tygor. DO NOT EDIT.

/**
 * CreateTaskRequest contains the data needed to create a new task.
 * @see github.com/broady/tygor/examples/config-showcase/api/types.go#CreateTaskRequest
 */
export interface CreateTaskRequest {
  title: string;
  description?: string;
  priority: Priority;
  assignee?: string;
  tags?: string[];
}
/**
 * ListTasksParams contains query parameters for listing tasks.
 * @see github.com/broady/tygor/examples/config-showcase/api/types.go#ListTasksParams
 */
export interface ListTasksParams {
  /** Status filters tasks by their current status. */
  status?: Status;
  /** Assignee filters tasks by assigned user. */
  assignee?: string;
  /** Limit is the maximum number of results. */
  limit?: number /* int */;
  /** Offset is the pagination offset. */
  offset?: number /* int */;
}
/**
 * Priority represents task urgency levels.
 * @see github.com/broady/tygor/examples/config-showcase/api/types.go#Priority
 */
export type Priority = 4 | 3 | 1 | 2;
/**
 * Status represents the state of a task.
 * @see github.com/broady/tygor/examples/config-showcase/api/types.go#Status
 */
export type Status = "cancelled" | "completed" | "in_progress" | "pending";
/**
 * Task represents a work item in the system.
 * @see github.com/broady/tygor/examples/config-showcase/api/types.go#Task
 */
export interface Task {
  /** ID is the unique identifier for the task. */
  id: string;
  /** Title is the task's headline. */
  title: string;
  /** Description is the detailed task description. */
  description?: string;
  /** Status is the current state of the task. */
  status: Status;
  /** Priority determines the task's urgency. */
  priority: Priority;
  /** Assignee is the user responsible for the task. */
  assignee?: string;
  /** DueDate is when the task should be completed. */
  due_date?: string /* RFC3339 */;
  /** Tags are labels for categorization. */
  tags?: string[];
  /** CreatedAt is when the task was created. */
  created_at: string /* RFC3339 */;
}
```

### EnumStyle: enum

<!-- [snippet-file:client/src/enum/types.ts] -->
```typescript title="types.ts"
// Code generated by tygor. DO NOT EDIT.

/**
 * CreateTaskRequest contains the data needed to create a new task.
 * @see github.com/broady/tygor/examples/config-showcase/api/types.go#CreateTaskRequest
 */
export interface CreateTaskRequest {
  title: string;
  description?: string;
  priority: Priority;
  assignee?: string;
  tags?: string[];
}
/**
 * ListTasksParams contains query parameters for listing tasks.
 * @see github.com/broady/tygor/examples/config-showcase/api/types.go#ListTasksParams
 */
export interface ListTasksParams {
  /** Status filters tasks by their current status. */
  status?: Status;
  /** Assignee filters tasks by assigned user. */
  assignee?: string;
  /** Limit is the maximum number of results. */
  limit?: number /* int */;
  /** Offset is the pagination offset. */
  offset?: number /* int */;
}
/**
 * Priority represents task urgency levels.
 * @see github.com/broady/tygor/examples/config-showcase/api/types.go#Priority
 */
export enum Priority {
  /** PriorityCritical is for immediate attention. */
  PriorityCritical = 4,
  /** PriorityHigh is for urgent tasks. */
  PriorityHigh = 3,
  /** PriorityLow is for non-urgent tasks. */
  PriorityLow = 1,
  /** PriorityMedium is the default priority. */
  PriorityMedium = 2,
}
/**
 * Status represents the state of a task.
 * @see github.com/broady/tygor/examples/config-showcase/api/types.go#Status
 */
export enum Status {
  /** StatusCancelled indicates the task was cancelled. */
  StatusCancelled = "cancelled",
  /** StatusCompleted indicates the task has been finished. */
  StatusCompleted = "completed",
  /** StatusInProgress indicates the task is currently being worked on. */
  StatusInProgress = "in_progress",
  /** StatusPending indicates the task is waiting to be started. */
  StatusPending = "pending",
}
/**
 * Task represents a work item in the system.
 * @see github.com/broady/tygor/examples/config-showcase/api/types.go#Task
 */
export interface Task {
  /** ID is the unique identifier for the task. */
  id: string;
  /** Title is the task's headline. */
  title: string;
  /** Description is the detailed task description. */
  description?: string;
  /** Status is the current state of the task. */
  status: Status;
  /** Priority determines the task's urgency. */
  priority: Priority;
  /** Assignee is the user responsible for the task. */
  assignee?: string;
  /** DueDate is when the task should be completed. */
  due_date?: string /* RFC3339 */;
  /** Tags are labels for categorization. */
  tags?: string[];
  /** CreatedAt is when the task was created. */
  created_at: string /* RFC3339 */;
}
```

### EnumStyle: const_enum

<!-- [snippet-file:client/src/const/types.ts] -->
```typescript title="types.ts"
// Code generated by tygor. DO NOT EDIT.

/**
 * CreateTaskRequest contains the data needed to create a new task.
 * @see github.com/broady/tygor/examples/config-showcase/api/types.go#CreateTaskRequest
 */
export interface CreateTaskRequest {
  title: string;
  description?: string;
  priority: Priority;
  assignee?: string;
  tags?: string[];
}
/**
 * ListTasksParams contains query parameters for listing tasks.
 * @see github.com/broady/tygor/examples/config-showcase/api/types.go#ListTasksParams
 */
export interface ListTasksParams {
  /** Status filters tasks by their current status. */
  status?: Status;
  /** Assignee filters tasks by assigned user. */
  assignee?: string;
  /** Limit is the maximum number of results. */
  limit?: number /* int */;
  /** Offset is the pagination offset. */
  offset?: number /* int */;
}
/**
 * Priority represents task urgency levels.
 * @see github.com/broady/tygor/examples/config-showcase/api/types.go#Priority
 */
export const enum Priority {
  /** PriorityCritical is for immediate attention. */
  PriorityCritical = 4,
  /** PriorityHigh is for urgent tasks. */
  PriorityHigh = 3,
  /** PriorityLow is for non-urgent tasks. */
  PriorityLow = 1,
  /** PriorityMedium is the default priority. */
  PriorityMedium = 2,
}
/**
 * Status represents the state of a task.
 * @see github.com/broady/tygor/examples/config-showcase/api/types.go#Status
 */
export const enum Status {
  /** StatusCancelled indicates the task was cancelled. */
  StatusCancelled = "cancelled",
  /** StatusCompleted indicates the task has been finished. */
  StatusCompleted = "completed",
  /** StatusInProgress indicates the task is currently being worked on. */
  StatusInProgress = "in_progress",
  /** StatusPending indicates the task is waiting to be started. */
  StatusPending = "pending",
}
/**
 * Task represents a work item in the system.
 * @see github.com/broady/tygor/examples/config-showcase/api/types.go#Task
 */
export interface Task {
  /** ID is the unique identifier for the task. */
  id: string;
  /** Title is the task's headline. */
  title: string;
  /** Description is the detailed task description. */
  description?: string;
  /** Status is the current state of the task. */
  status: Status;
  /** Priority determines the task's urgency. */
  priority: Priority;
  /** Assignee is the user responsible for the task. */
  assignee?: string;
  /** DueDate is when the task should be completed. */
  due_date?: string /* RFC3339 */;
  /** Tags are labels for categorization. */
  tags?: string[];
  /** CreatedAt is when the task was created. */
  created_at: string /* RFC3339 */;
}
```

### EnumStyle: object

<!-- [snippet-file:client/src/object/types.ts] -->
```typescript title="types.ts"
// Code generated by tygor. DO NOT EDIT.

/**
 * CreateTaskRequest contains the data needed to create a new task.
 * @see github.com/broady/tygor/examples/config-showcase/api/types.go#CreateTaskRequest
 */
export interface CreateTaskRequest {
  title: string;
  description?: string;
  priority: Priority;
  assignee?: string;
  tags?: string[];
}
/**
 * ListTasksParams contains query parameters for listing tasks.
 * @see github.com/broady/tygor/examples/config-showcase/api/types.go#ListTasksParams
 */
export interface ListTasksParams {
  /** Status filters tasks by their current status. */
  status?: Status;
  /** Assignee filters tasks by assigned user. */
  assignee?: string;
  /** Limit is the maximum number of results. */
  limit?: number /* int */;
  /** Offset is the pagination offset. */
  offset?: number /* int */;
}
/**
 * Priority represents task urgency levels.
 * @see github.com/broady/tygor/examples/config-showcase/api/types.go#Priority
 */
export const Priority = {
  /** PriorityCritical is for immediate attention. */
  PriorityCritical: 4,
  /** PriorityHigh is for urgent tasks. */
  PriorityHigh: 3,
  /** PriorityLow is for non-urgent tasks. */
  PriorityLow: 1,
  /** PriorityMedium is the default priority. */
  PriorityMedium: 2,
} as const;
/**
 * Status represents the state of a task.
 * @see github.com/broady/tygor/examples/config-showcase/api/types.go#Status
 */
export const Status = {
  /** StatusCancelled indicates the task was cancelled. */
  StatusCancelled: "cancelled",
  /** StatusCompleted indicates the task has been finished. */
  StatusCompleted: "completed",
  /** StatusInProgress indicates the task is currently being worked on. */
  StatusInProgress: "in_progress",
  /** StatusPending indicates the task is waiting to be started. */
  StatusPending: "pending",
} as const;
/**
 * Task represents a work item in the system.
 * @see github.com/broady/tygor/examples/config-showcase/api/types.go#Task
 */
export interface Task {
  /** ID is the unique identifier for the task. */
  id: string;
  /** Title is the task's headline. */
  title: string;
  /** Description is the detailed task description. */
  description?: string;
  /** Status is the current state of the task. */
  status: Status;
  /** Priority determines the task's urgency. */
  priority: Priority;
  /** Assignee is the user responsible for the task. */
  assignee?: string;
  /** DueDate is when the task should be completed. */
  due_date?: string /* RFC3339 */;
  /** Tags are labels for categorization. */
  tags?: string[];
  /** CreatedAt is when the task was created. */
  created_at: string /* RFC3339 */;
}
```

### OptionalType: default

<!-- [snippet-file:client/src/opt-default/types.ts] -->
```typescript title="types.ts"
// Code generated by tygor. DO NOT EDIT.

/**
 * CreateTaskRequest contains the data needed to create a new task.
 * @see github.com/broady/tygor/examples/config-showcase/api/types.go#CreateTaskRequest
 */
export interface CreateTaskRequest {
  title: string;
  description?: string | null;
  priority: Priority;
  assignee?: string | null;
  tags?: string[] | null;
}
/**
 * ListTasksParams contains query parameters for listing tasks.
 * @see github.com/broady/tygor/examples/config-showcase/api/types.go#ListTasksParams
 */
export interface ListTasksParams {
  /** Status filters tasks by their current status. */
  status?: Status | null;
  /** Assignee filters tasks by assigned user. */
  assignee?: string | null;
  /** Limit is the maximum number of results. */
  limit?: number /* int */ | null;
  /** Offset is the pagination offset. */
  offset?: number /* int */ | null;
}
/**
 * Priority represents task urgency levels.
 * @see github.com/broady/tygor/examples/config-showcase/api/types.go#Priority
 */
export type Priority = 4 | 3 | 1 | 2;
/**
 * Status represents the state of a task.
 * @see github.com/broady/tygor/examples/config-showcase/api/types.go#Status
 */
export type Status = "cancelled" | "completed" | "in_progress" | "pending";
/**
 * Task represents a work item in the system.
 * @see github.com/broady/tygor/examples/config-showcase/api/types.go#Task
 */
export interface Task {
  /** ID is the unique identifier for the task. */
  id: string;
  /** Title is the task's headline. */
  title: string;
  /** Description is the detailed task description. */
  description?: string | null;
  /** Status is the current state of the task. */
  status: Status;
  /** Priority determines the task's urgency. */
  priority: Priority;
  /** Assignee is the user responsible for the task. */
  assignee?: string | null;
  /** DueDate is when the task should be completed. */
  due_date?: string /* RFC3339 */ | null;
  /** Tags are labels for categorization. */
  tags: string[] | null;
  /** CreatedAt is when the task was created. */
  created_at: string /* RFC3339 */;
}
```

### OptionalType: null

<!-- [snippet-file:client/src/opt-null/types.ts] -->
```typescript title="types.ts"
// Code generated by tygor. DO NOT EDIT.

/**
 * CreateTaskRequest contains the data needed to create a new task.
 * @see github.com/broady/tygor/examples/config-showcase/api/types.go#CreateTaskRequest
 */
export interface CreateTaskRequest {
  title: string;
  description: string | null;
  priority: Priority;
  assignee: string | null;
  tags: string[] | null;
}
/**
 * ListTasksParams contains query parameters for listing tasks.
 * @see github.com/broady/tygor/examples/config-showcase/api/types.go#ListTasksParams
 */
export interface ListTasksParams {
  /** Status filters tasks by their current status. */
  status: Status | null;
  /** Assignee filters tasks by assigned user. */
  assignee: string | null;
  /** Limit is the maximum number of results. */
  limit: number /* int */ | null;
  /** Offset is the pagination offset. */
  offset: number /* int */ | null;
}
/**
 * Priority represents task urgency levels.
 * @see github.com/broady/tygor/examples/config-showcase/api/types.go#Priority
 */
export type Priority = 4 | 3 | 1 | 2;
/**
 * Status represents the state of a task.
 * @see github.com/broady/tygor/examples/config-showcase/api/types.go#Status
 */
export type Status = "cancelled" | "completed" | "in_progress" | "pending";
/**
 * Task represents a work item in the system.
 * @see github.com/broady/tygor/examples/config-showcase/api/types.go#Task
 */
export interface Task {
  /** ID is the unique identifier for the task. */
  id: string;
  /** Title is the task's headline. */
  title: string;
  /** Description is the detailed task description. */
  description: string | null;
  /** Status is the current state of the task. */
  status: Status;
  /** Priority determines the task's urgency. */
  priority: Priority;
  /** Assignee is the user responsible for the task. */
  assignee: string | null;
  /** DueDate is when the task should be completed. */
  due_date: string /* RFC3339 */ | null;
  /** Tags are labels for categorization. */
  tags: string[] | null;
  /** CreatedAt is when the task was created. */
  created_at: string /* RFC3339 */;
}
```

### OptionalType: undefined

<!-- [snippet-file:client/src/opt-undef/types.ts] -->
```typescript title="types.ts"
// Code generated by tygor. DO NOT EDIT.

/**
 * CreateTaskRequest contains the data needed to create a new task.
 * @see github.com/broady/tygor/examples/config-showcase/api/types.go#CreateTaskRequest
 */
export interface CreateTaskRequest {
  title: string;
  description?: string;
  priority: Priority;
  assignee?: string;
  tags?: string[];
}
/**
 * ListTasksParams contains query parameters for listing tasks.
 * @see github.com/broady/tygor/examples/config-showcase/api/types.go#ListTasksParams
 */
export interface ListTasksParams {
  /** Status filters tasks by their current status. */
  status?: Status;
  /** Assignee filters tasks by assigned user. */
  assignee?: string;
  /** Limit is the maximum number of results. */
  limit?: number /* int */;
  /** Offset is the pagination offset. */
  offset?: number /* int */;
}
/**
 * Priority represents task urgency levels.
 * @see github.com/broady/tygor/examples/config-showcase/api/types.go#Priority
 */
export type Priority = 4 | 3 | 1 | 2;
/**
 * Status represents the state of a task.
 * @see github.com/broady/tygor/examples/config-showcase/api/types.go#Status
 */
export type Status = "cancelled" | "completed" | "in_progress" | "pending";
/**
 * Task represents a work item in the system.
 * @see github.com/broady/tygor/examples/config-showcase/api/types.go#Task
 */
export interface Task {
  /** ID is the unique identifier for the task. */
  id: string;
  /** Title is the task's headline. */
  title: string;
  /** Description is the detailed task description. */
  description?: string;
  /** Status is the current state of the task. */
  status: Status;
  /** Priority determines the task's urgency. */
  priority: Priority;
  /** Assignee is the user responsible for the task. */
  assignee?: string;
  /** DueDate is when the task should be completed. */
  due_date?: string /* RFC3339 */;
  /** Tags are labels for categorization. */
  tags?: string[];
  /** CreatedAt is when the task was created. */
  created_at: string /* RFC3339 */;
}
```

### PreserveComments: none

<!-- [snippet-file:client/src/no-comments/types.ts] -->
```typescript title="types.ts"
// Code generated by tygor. DO NOT EDIT.

/** @see github.com/broady/tygor/examples/config-showcase/api/types.go#CreateTaskRequest */
export interface CreateTaskRequest {
  title: string;
  description?: string;
  priority: Priority;
  assignee?: string;
  tags?: string[];
}
/** @see github.com/broady/tygor/examples/config-showcase/api/types.go#ListTasksParams */
export interface ListTasksParams {
  status?: Status;
  assignee?: string;
  limit?: number /* int */;
  offset?: number /* int */;
}
/** @see github.com/broady/tygor/examples/config-showcase/api/types.go#Priority */
export type Priority = 4 | 3 | 1 | 2;
/** @see github.com/broady/tygor/examples/config-showcase/api/types.go#Status */
export type Status = "cancelled" | "completed" | "in_progress" | "pending";
/** @see github.com/broady/tygor/examples/config-showcase/api/types.go#Task */
export interface Task {
  id: string;
  title: string;
  description?: string;
  status: Status;
  priority: Priority;
  assignee?: string;
  due_date?: string /* RFC3339 */;
  tags?: string[];
  created_at: string /* RFC3339 */;
}
```

## File Structure

```
config-showcase/
├── main.go           # Custom generation logic
├── api/types.go      # Go types with comments
└── client/src/
    ├── union/        # EnumStyle: union
    ├── enum/         # EnumStyle: enum
    ├── const/        # EnumStyle: const_enum
    ├── object/       # EnumStyle: object
    ├── opt-default/  # OptionalType: default
    ├── opt-null/     # OptionalType: null
    ├── opt-undef/    # OptionalType: undefined
    └── no-comments/  # PreserveComments: none
```
